PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-key-change-in-production
# Replace <user>, <password>, <cluster> with your Atlas values.
# If password has special chars (@ # : / etc.), URL-encode them (e.g. @ → %40, # → %23).
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/boarding-student?retryWrites=true&w=majority
API_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173
