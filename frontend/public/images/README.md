# Logo

Place your project logo here as **`logo.png`** (or **`logo.svg`**).

It will be used:
- In the app header (mobile bar and desktop sidebar)
- As the browser tab icon (favicon)

Recommended: PNG or SVG, at least 32×32 px (or 64×64 for sharper display).
